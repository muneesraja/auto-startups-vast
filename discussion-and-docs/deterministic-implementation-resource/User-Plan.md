Revamping and producing new skill:
We are going to plan a new skill called story to video deterministic way, the current skill `story-to-video-cinematic`

Currently the entire flow is working on non deterministic way, the plan is same as before as below

````
Since we are going to use FFLF (First frame last frame) workflow the author has recursive way to bring smooth story continuation.
For ex. (Let's take scene 1),
Shot 1 FF: A panda walking in forest (the image will be produced by Ideogram 4 and edited with Flux klein 9b for character consistency)
Since it's the shot 1 we need to generate the FF
Shot 1 LF: The panda saw a butterfly in the scene (Since the author stated if we want smooth continuation, the FF and LF shouldn't be soo much different but it requires differenciation of the actions or a change in scene here the panda is walking so the visual shot of LF's camera should move forward, Here only Flux Klein 9b uses the FF and character sheet and produce it)
For the shot 1 we now have FF and LF.

Now coming to the shot 2 FF:
In previous phases before the agent starts generating images, the agent should decide if this shot 1 and shot 2 require continuity if so, the first video generation's last frame can be used as FF for the shot 2, if the agent earlier decided this shot doesn't require continuity in that case FF for this scene should follow SHOT produced formula where (Ideogram generates FF -> Consistency brought by Flux 2 klein FF with character sheet -> LF generated by FF and chracter sheet as reference of shot2).


Now coming to the technical part:
Since we have a recursive system in place we cannot do batch image generation where all FF and LF will be generated it all decided on if continuity is true or false. In this case, the plan is simple.
Agent work on batch mode for our scenario:
step 1:
generates character sheet
step 2:
Director phase, where a story is taken into process and reasons about direction and cinematograpphy, 
Important: Maximum of continous 3 shot for now can be decided as continous shots
for ex. 


Scene 1:
Shot 1: Panda walks in jungle
requires continuation
Shot 2 : Panda sees butterfly
##cut (Shot two and shot three doesn't require continuation, it requires a cut between frames)
Shot 3: Suddenly panda looks forward a shot of water fall appears
** continued below json thought process **


step 3: 
For all scenes first shot(FF) will generate in batch/bulk by ideogram 4 
Note: 
Some in between of shots need to cut and break to a different scene, refer ##cut
Step 4:
Now we have FF of all the scene, using that according to author's suggestion, there shouldn't be super great difference between FF and LF. So Flux klein 9b will be used to produce all the LF since it's a edit model it can change the viewing perspective, it can control camera, it can change the actions, it can change the expression, it can move the camera etc.
Step 5:
Video will now generated using FF and LF generated and we call it as batch_1, once batch_1 completed generation, all the end_frame or author's suggestion from the video will be extracted for continuous shot building.
Step 6:
Extracted images now will be the FF of the generation with it Flux 9B uses to generate LF and video will be generated
Step 7:
Video will now generated using FF and LF generated and we call it as batch_1, once batch_1 completed generation, all the end_frame or author's suggestion from the video will be extracted for continuous shot building.
Step 8:
Extracted images now will be the FF of the generation with it Flux 9B uses to generate LF.
Done!


** continued below json thought process, planning up the JSON and improving workflow to be dynamic, flux can support upto 5 images as references **
Issue to be identified, 
Strong reasoning needed in this phase as this is the pillar:
If you notice the shot for an example `/Users/muneesraja/Documents/growthlabs-vault/story-to-video-cinematic/panda-pippin/scenes_edited/panda-pippin_s08_sh01_lf_edited_00001_.png` the flux 2 klein 9b edit workflow is passed only with panda as reference, because of that the monkey in the scene became panda, for such situation where two character references should be passed along, workflow should dynamically attach more reference images according to scene or situations basically the workflow/api of flux 2 9b klein we are using should be scaled with the reference image. 

We may create a phase to our skill where it only generates a array of objects, with another json with a key for the characters, this reasoning should include director mode,

Characters
```

[ {
name: monkey
id: 1,
character_sheet_path: "/root/etc/etc"
...other_info
},
{
name: panda
id: 2,
character_sheet_path: "/root/etc/etc"
...other_info
]
```

Scenes:
```
{
scene_1: [{
shot_1: {
characters_present: []
...other_info
},
shot_2: {
characters_present: [2]
...other_info
},
shot_3: {
characters_present: [1,2]
...other_info
},
}]
}
```



````


But the major change here is everything currently is managed by the agent but now we are going to handover everything as a script and with custom system instruction it'll be handled by Openrouter's models for ex. gpt 4o mini.

New plan:
````
Since we are going to use FFLF (First frame last frame) workflow the author has recursive way to bring smooth story continuation.
For ex. (Let's take scene 1),
Shot 1 FF: A panda walking in forest (the image will be produced by Ideogram 4 and edited with Flux klein 9b for character consistency)
Since it's the shot 1 we need to generate the FF
Shot 1 LF: The panda saw a butterfly in the scene (Since the author stated if we want smooth continuation, the FF and LF shouldn't be soo much different but it requires differenciation of the actions and environment (Water flowing, wind) or a change in scene here the panda is walking so the visual shot of LF's camera should move forward, Here only Flux Klein 9b uses the FF and character sheet and produce it)
For the shot 1 we now have FF and LF.

Now coming to the shot 2 FF:
In previous phases before the agent starts generating images, the agent should decide if this shot 1 and shot 2 require continuity if so, the first video generation's last frame can be used as FF for the shot 2, if the agent earlier decided this shot doesn't require continuity in that case FF for this scene should follow SHOT produced formula where (Ideogram generates FF -> Consistency brought by Flux 2 klein FF with character sheet -> LF generated by FF and chracter sheet as reference of shot2).


Now coming to the technical part:
Since we have a recursive system in place we cannot do batch image generation where all FF and LF will be generated it all decided on if continuity is true or false. In this case, the plan is simple.
Agent work on batch mode for our scenario:

step 1:
Director agent (With Custom system prompt, refer - discussion-and-docs/deterministic-implementation-resource/AI-Film-making.md) which takes the user given story and rewrites the story in `Director_script.md` file. Keeping the FFLF in mind, also since the author set a mandatory rules if a shot is minor for ex. turning the head towards the camera for such scenario the duration should be super shot else the LTX FFLF will starts hallucinate the output video results, while writing each scene and shot the director should concentrate every aspects of the shot's envionment and characters. How a character/subject should look, Style/Aesthetic of the story, lightning, backgrounds, crowd and third person characters.
Important: 
- Maximum of continous 3 shot in sequence for now can be decided as continous shots
- The agent should also reason about how a shot of FF image can be altered so that how camera should be panned in LF, how particles in LF should appear compared to FF.
- Most importantly, duration of scene if small shot 2s to 8s maximum (Very important)
Director's quick notes:
Scene 1:
Shot 1: Panda walks in jungle
requires continuation
Shot 2 : Panda sees butterfly
##cut (Shot two and shot three doesn't require continuation, it requires a cut between frames)
Shot 3: Suddenly panda looks forward a shot of water fall appears



step 2 (Intelligent model should be used via Openrouter):
Director agent (With Custom system prompt, refer - discussion-and-docs/deterministic-implementation-resource/AI-Film-making.md) now takes the `Director_script.md` as context and produces a JSON file called director_visual_blueprint.json which should contains total duration, 
 - splits scene by scene duration, 
 - shot by shot duration, 
 - if a scene requires cut or continuoation from previous shot boolean,
 - dialogue's if any. 
 - How a start frame should look like 
 - how a end frame should look like for each shot, 
 - what are all the changes between FF and LF image should be there. 
 - Characters to be generated as character sheet an array of object charcter's appearance, charcters.
 - Most importantly which characters should be present in which shot in an array with character ID (Later this context is used by Flux klein 9b for editing by reference image for consistency). 
Note: The agent should also reason about how a shot of FF image can be altered so that how camera should be panned in LF, how particles in LF should appear compared to FF.
- Charcter expression if any for FF and LF
  



step 3:
Use the `director_visual_blueprint.json` Prompt enhancer character sheet with ideogram (With Custom system prompt - refer - discussion-and-docs/deterministic-implementation-resource/ideogram-character-sheet.json),
This agent's only task is to create prompt specifically for generating character sheet and store it in prompts.json. Once generated the file name with file path and file name should be updated to `director_visual_blueprint.json` so other agents or script can be refer this.
Note:
prompts.json should be contained with the reference image paths.


Step 4:
Use the `director_visual_blueprint.json`, FF shot prompter with ideogram With custom instruction (refer discussion-and-docs/deterministic-implementation-resource/ideogram-4-prompting-guide.md), this agent's only task is to create prompt for FF shots and update the prompts.json. Once generated the file name with file path and file name should be updated to `director_visual_blueprint.json` so other LF agents or LF script can be refer this.
Note:
prompts.json should be contained with the reference image paths.


Step 5:
Consistency prompt enhancer using FLux klein 9B with custom instruction specifically to edit based prompting style (refer discussion-and-docs/deterministic-implementation-resource/FLUX-prompting-guide), this takes the FF prompt and creates an edit prompt which applies patch on top of generated outputs. For charcter consistency we will be using Flux klein 9B as an edit model and characters that needs to be in the scene will be in the `director_visual_blueprint.json`, which gets attached the reference character sheet based on shot/scene which will be available in the `director_visual_blueprint.json`, for ex. 
Flux 
shot 1 a tortoise walking with deer (references attached are Tortoise and Deer)
shot 2 a wide angle shot of the jungle (No reference empty array)
shot 3 the tortoise, lion, deer and hen playing cards at middle of the jungle (references attached, tortoise, lion, deer, hen - Flux supports 4 reference inputs)
Note: 
- This step also include character expression to be transfered from the source prompt when applying consistency filter using Flux klein 9B
- prompts.json should be contained with the reference image paths.


Step 6:
Using the `director_visual_blueprint.json` LF shot prompter with Flex klein 9B with custom instruction specifically to edit based prompting style (refer discussion-and-docs/deterministic-implementation-resource/FLUX-prompting-guide) and gets update the prompts.json.
The reasoning needed are how FF frame alings, how a 
Camera: camera can be tilted or pan or zoomed in or zoomed out, or camera moves forward
Scene: How background object changes based on FF, how trees moves in the wind, how water flows changes can be done in LF, which direction the character to look at, head position, action changes, facial changes.
Consistency.
Note:
prompts.json should be contained with the reference image paths.


Step 7:
Using the `director_visual_blueprint.json` and `discussion-and-docs/deterministic-implementation-resource/AI-Film-making.md` as context, motion prompt should be generated and stored in the prompts.json.
Note:
prompts.json should be contained with the reference image paths.

Step 8: organizer/categorize script should create a generator.json by running a loop in prompts.json:
At a higer level generator_wave_1.json looks something like:
[
{
id: "batch_1",
shots: ["scene_1_shot_1", "scene_1_shot_4", "scene_2_shot_1", "scene_3_shot_1"]
}
]
Assume there are 3 scenes, the filter checks one condition `continuation: false`, the first scene first shot never has this true since it's the starting point, and if director agent plans to cut a continuation:

## Wave 1
###1. Updates generator_wave_1.json FF image generations for wave 1 from prompts.json
scene 1 shot 1 FF image from prompts.json
scene 1 shot 4 FF image from prompts.json
scene 2 shot 1 FF image from prompts.json
Scene 3 shot 1 FF image from prompts.json
###2. Updates generator_wave_1.json Consistency patcher using Flux 9b edit for wave 1 from prompts.json
scene 1 shot 1 FF image from prompts.json
scene 1 shot 4 FF image from prompts.json
scene 2 shot 1 FF image from prompts.json
Scene 3 shot 1 FF image from prompts.json
###3. Updates generator_wave_1.json LF image generations for wave 1 from prompts.json (Make sure it includes these reference images, FF image, character sheet 1, character sheet 2 (Character sheet references are dynamic in nature))
scene 1 shot 1 LF image from prompts.json
scene 1 shot 4 LF image from prompts.json
scene 2 shot 1 LF image from prompts.json
Scene 3 shot 1 LF image from prompts.json
###4. Updates generator_wave_1.json motion prompts for wave 1 prompts.json
scene 1 shot 1 LF image from prompts.json
scene 1 shot 4 LF image from prompts.json
scene 2 shot 1 LF image from prompts.json
Scene 3 shot 1 LF image from prompts.json

Step 9: Runs the generator_wave_1.json, once the video is generated

## Wave 2
###1. Updates generator_wave_1.json FF image generations for wave 1 from prompts.json

To-Do: Pending for now. Pending until Wave 1 was tested properly





````
