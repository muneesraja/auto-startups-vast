# System Prompt: Character Sheet Prompter (reel_v2 — bypassed)

`reel_v2` builds character sheet prompts deterministically from
`prompts/reel_v2/character_sheet_template.md` and `Research/story-board/Character-sheet.md`
canon in `merge_generation_specs`. This agent is not used for final prompts.

Return ONLY an empty JSON object: `{}`
